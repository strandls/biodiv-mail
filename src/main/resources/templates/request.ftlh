<table width="100%" border="0" cellspacing="0" cellpadding="0" style="width: 100% !important" ><tbody><tr><td align="center"><table style=" border: 1px solid #d1d1d1; border-radius: 5px; margin: 40px 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; color: #2d3748; " width="600" border="0" cellspacing="0" cellpadding="0" ><tbody><tr><td align="center" style="padding: 0;"><div style=" text-align: left; width: 100%; font-size: 18px; line-height: 1.4; " ><div style="padding: 40px 60px"><p>Hi ${name},</p><p><a href="${serverUrl}/user/show/${requestor.id?c}">${requestor.name}</a> is requesting to be a member of the group <a href=" <#if group.webAddress?starts_with("/group")> ${serverUrl}${group.webAddress}/show <#else> ${group.webAddress} </#if> ">${group.name}</a> which you are an administrator of on the ${siteName}. </p><a href=" <#if group.webAddress?starts_with("/group")> ${serverUrl}${group.webAddress}/continue/verify-request?token=${encrypted_key} <#else> ${group.webAddress}/continue/verify-request?token=${encrypted_key} </#if> " style=" background-color: #38A169; border-radius: 5px; color: #fff; display: inline-block; line-height: 50px; text-align: center; text-decoration: none; width: 180px; " >Approve Request</a><p>To deny, you need not perform any action.</p><p> Regards,<br/> The ${siteName} Team </p></div></div></td></tr></tbody></table></td></tr></tbody></table>